"use client";

import { CaptainMakerPanel } from "@/components/public/CaptainMakerPanel";

/** Homepage embed — full chat + estimate panel. */
export function CaptainMakerGuide() {
  return <CaptainMakerPanel variant="embedded" />;
}
