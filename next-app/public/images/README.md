# Site images

The primary app/site logo is **`/m3-brand.png`** at the `public/` root (1024×1024 source). Use that path in nav and metadata; do not reintroduce `massive-brain-m3.png` or `assets/logo.png` for brand marks.
