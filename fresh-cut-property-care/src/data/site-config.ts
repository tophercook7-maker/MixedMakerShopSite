/** Barrel for site + client discovery (import from here in app code if you prefer a single path). */
export { getActiveClient, getActiveClientKey, CLIENTS } from "./clients";
export { getResolvedSite, pageUrl } from "../lib/get-site-config";
