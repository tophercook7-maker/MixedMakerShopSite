# Active client profile (see src/data/clients/index.ts)
PUBLIC_SITE_CLIENT=fresh-cut-property-care

# Must match ClientConfig.siteUrl for canonical URLs + sitemap + OG URLs
PUBLIC_SITE_URL=https://freshcutpropertycare.com
