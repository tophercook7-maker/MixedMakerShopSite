{
  "name": "mixedmakershop",
  "version": "1.0.0",
  "description": "MixedMakerShop website",
  "scripts": {
    "dev": "cd next-app && npm run dev",
    "build": "cd next-app && npm run build",
    "start": "cd next-app && npm run start"
  },
  "dependencies": {
    "stripe": "^16.12.0",
    "twilio": "^4.23.0"
  }
}
